services:
  deno_server:
    build: .
    ports:
      - "8080:8000"
    command: ["deno", "task", "dev"]
    volumes:
      - .:/app

    environment:
      DENO_ENV: development
      DENO_FUTURE_WATCH_POLLING: 1
    restart: always